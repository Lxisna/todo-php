{% extends 'base.twig' %}

{% block content %}

	<div class="container py-5">
		<div class="my-1">
			<a href="/todo_list/public/task/"></a>
		</div>
		<div class="card">
			<div class="card-body">
				<div
					class="d-flex">
					{# <p>{{task.taskId}}</p> #}
					<div>{{task.title}}</div>
					<div class="px-2">
						<span class="badge text-bg-warning">{{task.status}}</span>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
